<div class='card mb-4'>
    <div class='card-body'>
        {{title}}
        <h5 class='card-title'>{{name}} - contato: {{email}}</h5>
        {{price}}
        {{description}}
        {{address}}
        {{image}}
        {{publication}}
    </div>
    <div class="col-6 ms-3">
        <a href="{{updateURL}}" class="btn btn-primary mb-3">Alterar</a>
        <a href="{{deleteURL}}" class="btn btn-danger mb-3">Excluir</a>
    </div>
</div>